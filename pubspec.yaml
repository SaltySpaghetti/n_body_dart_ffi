name: n_body_dart_ffi
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  ffi_c_plugin:
    path: plugins/ffi_c_plugin

  ffi_rust_plugin:
    path: plugins/ffi_rust_plugin

  vector_math: ^2.1.4
  material_design_icons_flutter: ^7.0.7296
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.3

flutter:
  assets:
    - assets/dart.png
    - assets/rust.png
    - assets/cpp.png
  uses-material-design: true
